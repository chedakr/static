{
  "name": "cheda-static",
  "version": "0.0.0",
  "scripts": {
    "build": "vite build",
    "deploy": "gh-pages -d dist --nojekyll"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.8"
  }
}
